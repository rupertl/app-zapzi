#!/usr/bin/env perl
use utf8;
use strict;
use warnings;

use App::Zapzi;
# VERSION
# ABSTRACT: store articles and publish them to read later
# PODNAME: zapzi

my $app = App::Zapzi->new();
$app->process_args(@ARGV);
exit $app->run;
